/*
Name: Sehan Madusha
Student ID: 106223861

Purpose:
This class represents an International Tour
in the Travel Australia Booking System.

This class inherits from the Tour class
and implements the Insurance interface.

Features:
- Stores international tour details
- Stores boarding airport information
- Stores pickup cost
- Stores insurance details
- Stores customer details
- Calculates total tour price including insurance
*/

public class InternationalTour extends Tour implements Insurance {

	private String insuranceType;
	private double insurancePrice;
	
	// customer object associated with booking
    private Customer customer;
    private String boardingAirport;
    private double pickupCost;
    
    //Constructor Used to create an InternationalTour object
    public InternationalTour(
            String tourName,
            double adultPrice,
            double childPrice,
            int adultCount,
            int childCount,
            String boardingAirport,
            double pickupCost,
            String insuranceType,
            double insurancePrice,
            String salesExecutiveName,
            Customer customer
    ) {

    	// calling parent class constructor
        super(
                tourName,
                adultPrice,
                childPrice,
                adultCount,
                childCount,
                salesExecutiveName
        );

        this.boardingAirport = boardingAirport;
        this.pickupCost = pickupCost;
        this.customer = customer;
        this.insuranceType = insuranceType;
        this.insurancePrice = insurancePrice;
        
        

    }

    // Displays full international tour details
    @Override
    public void displayTour() {

    	System.out.println("========================");
        System.out.println("Tour Name: "+ getTourName());
        System.out.println("Boarding Airport: "+ boardingAirport);
        System.out.println("Pickup Cost: "+ pickupCost);
        
        System.out.println("Adult Count: "+ getAdultCount());
        System.out.println("Child Count: "+ getChildCount());
        
        
        if(insurancePrice > 0) {
            System.out.println("Insurance Type: " + insuranceType);
            System.out.println("Insurance Price: " + insurancePrice);
        }
        System.out.println("Sales Executive: " + getSalesExecutiveName());
        
        System.out.println("Ticket Number: "+ getTicketNumber());
        
        // check whether booking is cancelled
        if(isCancelled()) {

            System.out.println("STATUS: CANCELLED");

        }
        System.out.printf("Total Price: %.2f\n",calculatePrice());
        customer.displayCustomer();
        System.out.println("========================");
        System.out.println();

    }

    //Calculates total tour price including insurance and pickup cost
    @Override
    public double calculatePrice() {

    	// calculate total ticket amount
        double total = (getAdultPrice() * getAdultCount())+(getChildPrice() * getChildCount());

        total=applyDiscount(total);
        return total + pickupCost + insurancePrice;

    }

    @Override
    public double calculateInsurance() {
        return insurancePrice;
    }

	

}
